name: probes
on:
  schedule:
    - cron: "*/10 * * * *"
jobs:
  e2e:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run probes
        run: node probes/run_probes.mjs
        env:
          BASE: ${{ secrets.CANARY_BASE }}
