# Edge Function env for rank-loads
SUPABASE_URL="https://<your-ref>.supabase.co"
# Optional: if invoking server-to-server. If absent, function should be invoked with user JWT.
SUPABASE_SERVICE_ROLE_KEY="<service-role-key>"
