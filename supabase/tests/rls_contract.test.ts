// Pseudocode outline to validate RLS per role/org
Deno.test("RLS visibility differs by role/org", async () => {
  // TODO: mint sessions for two orgs and roles; query loads_visible; assert row counts differ as expected
});
